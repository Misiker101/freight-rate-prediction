"""

Loads the trained model and produces:
  1. validation_predictions.csv  (load_id, predicted_rate for all 12,000 loads)
  2. data/december_chart_inputs.csv filled in-place with predicted_rate,
     saved as outputs/december_chart_inputs_filled.csv

"""
